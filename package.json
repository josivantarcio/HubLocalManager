{
  "name": "hublocal-manager",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend:start": "cd backend && npm run start:dev",
    "backend:start:prod": "cd backend && npm run start:prod",
    "backend:migrate": "cd backend && npm run migrate",
    "backend:generate:migration": "cd backend && npm run migration:generate",
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/josivantarcio/HubLocalManager.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/josivantarcio/HubLocalManager/issues"
  },
  "homepage": "https://github.com/josivantarcio/HubLocalManager#readme",
  "description": "HubLocal Manager - Sistema de gerenciamento de empresas e filiais",
  "devDependencies": {
    "@aws-amplify/backend": "^1.16.0",
    "@aws-amplify/backend-cli": "^1.7.0",
    "aws-cdk-lib": "^2.189.1",
    "constructs": "^10.4.2",
    "esbuild": "^0.25.3",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "aws-amplify": "^6.14.3"
  }
}
